<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Todoメモアプリ</title>

  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- favicon & icons -->
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">
  <link rel="stylesheet" href="/style.css">

  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            main: '#2563eb',  // 優しい青
            bg: '#f9fafb',    // 柔らかいグレー
            card: '#ffffff',
            accent: '#93c5fd'
          },
          boxShadow: {
            soft: '0 2px 8px rgba(0,0,0,0.06)'
          }
        }
      }
    }
  </script>
</head>

<body class="bg-bg text-gray-800 flex flex-col min-h-screen font-sans">

  <!-- ヘッダー -->
  <header class="bg-card shadow-soft py-3 px-4 flex items-center justify-center sticky top-0 z-10">
    <img src="/logo.svg" alt="Logo" class="h-9 w-auto">
  </header>

  <!-- メインコンテンツ -->
  <main class="flex-1 flex flex-col items-center justify-start px-4 py-8 sm:py-12">
    <div class="w-full max-w-md bg-card rounded-2xl shadow-soft p-6 sm:p-8">
      
      <form action="/add" method="post" class="flex mb-6">
        <input
          type="text"
          name="content"
          placeholder="やることを入力..."
          required
          class="flex-1 border border-gray-300 rounded-l-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-main"
        >
        <button
          type="submit"
          class="bg-main text-white px-5 rounded-r-lg hover:bg-accent transition font-medium text-sm"
        >
          追加
        </button>
      </form>

      <% if @todos.empty? %>
        <p class="text-center text-gray-400 italic py-6">まだ何もありません。</p>
      <% else %>
        <ul class="space-y-3">
          <% @todos.each_with_index do |todo, i| %>
            <li class="flex justify-between items-center bg-gray-50 border border-gray-200 rounded-xl px-4 py-3 hover:bg-gray-100 transition">
              <span class="truncate text-sm"><%= todo %></span>
              <form action="/delete" method="post" class="ml-3">
                <input type="hidden" name="index" value="<%= i %>">
                <button
                  type="submit"
                  class="text-xs bg-red-500 text-white px-3 py-1 rounded-md hover:bg-red-600 transition"
                >
                  削除
                </button>
              </form>
            </li>
          <% end %>
        </ul>
      <% end %>
    </div>
  </main>

  <!-- フッター -->
  <footer class="text-gray-400 text-center text-xs py-4">
    © 2025 Todo Memo App
  </footer>

</body>
</html>