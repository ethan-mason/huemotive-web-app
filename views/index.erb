<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Huemotive</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">
  <link rel="stylesheet" href="/style.css">

  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            main: '#007bff',
            bg: '#f9fafb',
            card: '#ffffff',
            accent: '#93c5fd'
          },
          boxShadow: {
            soft: '0 2px 8px rgba(0,0,0,0.06)'
          }
        }
      }
    }
  </script>
</head>

<body class="bg-bg text-gray-800 flex flex-col min-h-screen">

  <header class="bg-card shadow-soft p-4 flex items-center justify-center sticky top-0 z-10">
    <img src="/logo.svg" alt="Logo" class="h-8 w-auto">
  </header>

  <main class="flex-1 flex flex-col items-center justify-start px-4 md:px-0 py-6 sm:py-8">
    <div class="w-full max-w-md">
      <form action="/add" method="post" class="flex mb-6">
        <input
          type="text"
          name="content"
          placeholder="Enter a task..."
          required
          class="h-10 flex-1 border border-gray-200 rounded-l-lg rounded-r-none px-4 py-2 placeholder:text-sm outline-none"
        >
        <button
          type="submit"
          class="bg-main text-white px-4 py-2 rounded-r-lg hover:bg-accent transition font-medium"
        >
          Add
        </button>
      </form>

      <% if @todos.empty? %>
        <p class="text-center text-gray-400 text-lg py-4">No tasks yet.</p>
      <% else %>
        <ul class="space-y-3">
          <% @todos.each_with_index do |todo, i| %>
            <li class="flex justify-between items-center bg-card border border-gray-200 rounded-xl px-4 py-3 hover:bg-gray-100 transition">
              <span class="truncate text-sm"><%= todo %></span>
              <form action="/delete" method="post" class="ml-3">
                <input type="hidden" name="index" value="<%= i %>">
                <button
                  type="submit"
                  class="text-xs bg-red-500 text-white px-3 py-1 rounded-md hover:bg-red-600 transition"
                >
                  Delete
                </button>
              </form>
            </li>
          <% end %>
        </ul>
      <% end %>
    </div>
  </main>

  <!-- フッター -->
  <footer class="text-gray-400 text-center text-sm py-4">
    &copy; 2025 Huemotive. All rights reserved.
  </footer>

</body>
</html>