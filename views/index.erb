<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Huemotive</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="/tailwind-config.js"></script>
  <script src="/main.js"></script>
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">
  <link rel="stylesheet" href="/style.css">
  <style>
    input[type="range"] {
      -webkit-appearance: none;
      appearance: none;
      height: 8px;
      background: #e5e7eb;
      border-radius: 99em;
      outline: none;
    }
    input[type="range"]::-webkit-slider-thumb {
      -webkit-appearance: none;
      appearance: none;
      width: 18px;
      height: 18px;
      background: var(--main);
      border-radius: 99em;
      cursor: pointer;
      transition: background 0.2s, transform 0.2s;
    }
    input[type="range"]::-moz-range-thumb {
      width: 18px;
      height: 18px;
      background: var(--main);
      border-radius: 99em;
      cursor: pointer;
      transition: background 0.2s, transform 0.2s;
      border: none;
    }
  </style>
</head>

<body class="min-h-screen flex flex-col items-center text-gray-900 bg-[radial-gradient(ellipse_at_top_left,rgba(0,123,255,0.03),rgba(255,255,255,0.9))] transition-colors duration-400">

  <header class="w-full flex flex-col items-center py-6 px-4 md:px-0">
    <div class="w-full max-w-md flex items-center justify-between">
      <img src="/logo.svg" alt="Logo" class="h-6 w-fit">
      <div class="flex items-center">
        <label for="roundedRange" class="text-sm text-gray-600 mr-2">Rounded</label>
        <input type="range" min="0" max="32" value="8" id="roundedRange" class="w-24">
      </div>
    </div>
  </header>

  <main class="w-full max-w-md flex flex-col items-center px-4 md:px-0">

    <section class="w-full flex justify-center">
      <div id="colorPalette" class="flex gap-4">
        <% colors = [
          { hex: "#007bff", name: "Blue" },
          { hex: "#22c55e", name: "Green" },
          { hex: "#f97316", name: "Orange" },
          { hex: "#ec4899", name: "Pink" },
          { hex: "#6366f1", name: "Indigo" }
        ] %>

        <% colors.each do |color| %>
          <button
            class="relative w-8 h-8 aspect-square rounded-full border border-gray-200 hover:scale-105 transition-all duration-200 outline-none focus-visible:ring-2 focus-visible:ring-main focus-visible:ring-offset-2 <%= 'ring-2 ring-main ring-offset-2' if color[:hex] == '#007bff' %>"
            data-color="<%= color[:hex] %>"
            style="background: <%= color[:hex] %>;"
          ></button>
        <% end %>
      </div>
    </section>

    <section class="w-full">
      <% if @todos.empty? %>
        <p class="text-center text-gray-400 my-8">No tasks yet.</p>
      <% else %>
        <ul id="todoList" class="space-y-2 mt-8">
          <% @todos.each_with_index do |todo, i| %>
            <li class="group flex justify-between items-start bg-white border px-4 py-3 hover:bg-gray-50 transition-colors rounded-[var(--rounded)]">
              <div class="flex items-center">
                <div class="flex items-center">
                  <input
                    type="checkbox"
                    class="task-checkbox accent-main w-4 h-4 cursor-pointer rounded-sm"
                    id="todo-<%= i %>"
                  >
                  <label
                    for="todo-<%= i %>"
                    class="ml-2 break-words whitespace-normal cursor-pointer transition-colors"
                  >
                    <%= todo %>
                  </label>
                </div>
              </div>
              <form action="/delete" method="post" class="ml-3">
                <input type="hidden" name="index" value="<%= i %>">
                <button
                  type="submit"
                  class="text-sm text-red-500 hover:text-red-600 transition font-medium"
                >
                  Delete
                </button>
              </form>
            </li>
          <% end %>
        </ul>
      <% end %>
    </section>
  </main>

  <button
    id="openModalBtn"
    class="fixed bottom-8 left-1/2 -translate-x-1/2 bg-main text-white px-6 py-3 shadow-md hover:brightness-110 transition-all font-medium rounded-[var(--rounded)]"
  >
    Add a new task
  </button>

  <div id="taskModal" class="hidden fixed inset-0 bg-black/40 flex items-center justify-center z-50 backdrop-blur-sm">
    <div class="bg-white shadow-lg w-3/4 md:w-1/4 p-6 relative border rounded-[var(--rounded)]">
      <div class="flex mb-4 items-start">
        <h2 class="text-lg font-medium">Add a New Task</h2>
        <button id="closeModalBtn" class="ml-auto text-gray-400 hover:text-gray-600">✕</button>
      </div>
      <form action="/add" method="post" class="flex flex-col">
        <input type="text" name="content" placeholder="Enter your task..." required class="border rounded-[var(--rounded)] px-4 py-2 placeholder:text-sm outline-none">
        <div class="flex justify-end gap-3 mt-4">
          <button
            type="button"
            id="cancelBtn"
            class="px-4 py-2 border hover:bg-gray-50 transition rounded-[var(--rounded)]"
          >
            Cancel
          </button>
          <button
            type="submit"
            class="bg-main text-white px-4 py-2 hover:brightness-110 transition font-medium rounded-[var(--rounded)]"
          >
            Add
          </button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>