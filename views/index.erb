<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Huemotive</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="/tailwind-config.js"></script>
  <script src="/theme.js"></script>
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">
  <link rel="stylesheet" href="/style.css">
</head>

<body class="bg-bg text-gray-800 flex flex-col min-h-screen">

  <%= partial :header %>

  <main class="flex-1 flex flex-col items-center justify-start px-4 md:px-0 py-6 md:py-8">
    <div class="w-full max-w-md">
      <div class="flex justify-center gap-3 mb-6">
        <button class="w-6 h-6 rounded-full border border-gray-200 bg-[#007bff]" data-color="#007bff"></button>
        <button class="w-6 h-6 rounded-full border border-gray-200 bg-[#22c55e]" data-color="#22c55e"></button>
        <button class="w-6 h-6 rounded-full border border-gray-200 bg-[#ec4899]" data-color="#ec4899"></button>
        <button class="w-6 h-6 rounded-full border border-gray-200 bg-[#f97316]" data-color="#f97316"></button>
        <button class="w-6 h-6 rounded-full border border-gray-200 bg-[#6366f1]" data-color="#6366f1"></button>
      </div>
      <form action="/add" method="post" class="flex mb-6">
        <input
          type="text"
          name="content"
          placeholder="Enter a task..."
          required
          class="h-10 flex-1 border border-gray-200 rounded-l-lg rounded-r-none px-4 py-2 placeholder:text-sm outline-none"
        >
        <button
          type="submit"
          class="bg-main text-white px-4 py-2 rounded-r-lg hover:bg-accent transition font-medium"
        >
          Add
        </button>
      </form>

      <% if @todos.empty? %>
        <p class="text-center text-gray-400 text-lg py-4">No tasks yet.</p>
      <% else %>
        <ul class="space-y-3">
          <% @todos.each_with_index do |todo, i| %>
            <li class="flex justify-between items-center bg-card border border-gray-200 rounded-xl px-4 py-3 hover:bg-gray-100 transition">
              <span class="truncate text-sm"><%= todo %></span>
              <form action="/delete" method="post" class="ml-3">
                <input type="hidden" name="index" value="<%= i %>">
                <button
                  type="submit"
                  class="text-xs bg-red-500 text-white px-3 py-1 rounded-md hover:bg-red-600 transition"
                >
                  Delete
                </button>
              </form>
            </li>
          <% end %>
        </ul>
      <% end %>
    </div>
  </main>

  <%= partial :footer %>

</body>
</html>