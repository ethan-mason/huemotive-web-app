<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>Scribbler</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/style.css">
</head>
<body class="<%= theme_classes[:body] %>">
  <div class="max-w-md w-full px-6 md:px-0 py-6 mx-auto">

    <!-- ヘッダー -->
    <div class="flex items-center justify-between mb-6">
      <h1 class="text-2xl font-semibold">Scribbler</h1>
      <form action="/edit_mode" method="post">
      <button class="px-3 py-1 rounded-full text-sm
      <%= theme == 'light' ? 'bg-slate-200 text-slate-600' : 'bg-slate-600 text-slate-200' %>">
      Edit
      </button>
      </form>
    </div>

    <!-- テーマ切替ボタン -->
    <div class="flex gap-2 mb-6">
    <% THEMES.keys.each do |t| %>
      <form action="/theme/<%= t %>" method="post" class="flex-1">
        <button class="w-full px-3 py-2 rounded-lg text-sm font-medium
          <% if theme == 'light' %>
            <%= theme == t ? 'bg-slate-700 text-white' : 'bg-slate-200 text-slate-700' %>
          <% else %>
            <%= theme == t ? 'bg-slate-200 text-slate-900' : 'bg-slate-700 text-slate-200' %>
          <% end %>">
          <%= t.capitalize %>
        </button>
      </form>
    <% end %>
  </div>

    <!-- Editフォーム -->
    <% if edit_mode? %>
      <% THEMES.keys.each do |t| %>
        <form action="/update_theme/<%= t %>" method="post" class="mb-6 p-4 rounded-lg space-y-3 <%= theme_classes[:todo] %>">
          <h2 class="font-semibold mb-2"><%= t.capitalize %> Theme</h2>
          <% edit_theme(t).each do |key, value| %>
            <label class="block text-sm">
              <span><%= key %></span>
              <input type="text" name="classes[<%= key %>]" value="<%= value %>"
                class="mt-1 w-full px-3 py-2 rounded-md border border-slate-200 placeholder:text-slate-400">
            </label>
          <% end %>
          <button type="submit" class="w-full px-4 py-2 text-white rounded-md font-semibold <%= theme_classes[:button] %>">
            保存
          </button>
        </form>
      <% end %>
    <% end %>

    <!-- Todo追加フォーム -->
    <form action="/add" method="post" class="flex mb-6 gap-2">
      <input type="text" name="content" placeholder="やることを入力"
        class="flex-1 px-4 py-2 rounded-lg border focus:outline-none focus:ring-1 focus:ring-blue-400 <%= theme_classes[:input] %>">
      <button type="submit" class="px-4 py-2 rounded-full font-semibold <%= theme_classes[:button] %>">
        追加
      </button>
    </form>

    <!-- Todoリスト -->
    <ul class="space-y-3">
      <% todos.each do |todo| %>
        <li class="flex justify-between items-center px-4 py-3 rounded-lg shadow-sm <%= theme_classes[:todo] %>">
          <span class="break-words"><%= todo[:content] %></span>
          <form action="/delete/<%= todo[:id] %>" method="post">
            <button type="submit" class="px-2 text-sm py-1 text-red-400 font-semibold rounded-full hover:bg-red-200 duration-200">
              削除
            </button>
          </form>
        </li>
      <% end %>
    </ul>
  </div>
</body>
</html>
